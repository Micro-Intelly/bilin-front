import { SafeDataDirectiveDirective } from './safe-data-directive.directive';

describe('SafeDataDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeDataDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
